using System.ComponentModel;

namespace Desic.Application.Common.Models;

[Description("Each non-empty property will apply a filter potentially impacting the items that are returned. Note that only items matching all applied filters are returned (i.e. logical AND of the filter conditions).")]
public class Filter
{
}
