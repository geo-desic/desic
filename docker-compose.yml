services:
  desic.api:
    image: ${DOCKER_REGISTRY-}desicapi
    build:
      context: .
      dockerfile: src/Desic.Api/Dockerfile

  desic.web:
    image: ${DOCKER_REGISTRY-}desicweb
    build:
      context: src/Desic.Web
      dockerfile: Dockerfile
    command: sh -c "npm start"
    depends_on:
      - desic.api
